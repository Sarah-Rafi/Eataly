import 'package:flutter/material.dart';

class Party extends StatelessWidget {
  const Party({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      home: Center(child: Text("Party")),
    );
  }
}
